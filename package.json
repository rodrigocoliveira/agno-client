{
  "name": "agno-client-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Independent open-source client library for Agno agents",
  "author": "rodrigocoliveira",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/types build && bun run --cwd packages/core build && bun run --cwd packages/react build",
    "dev": "bun run --filter './packages/*' dev",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules bun.lock",
    "typecheck": "bun run --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "shadcn": "^3.5.0",
    "typescript": "^5.3.3"
  }
}
