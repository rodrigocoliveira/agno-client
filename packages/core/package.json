{
  "name": "@rodrigocoliveira/agno-client",
  "version": "1.0.2",
  "description": "Core client library for Agno agents with streaming support and HITL frontend tool execution",
  "author": "rodrigocoliveira",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rodrigocoliveira/agno-client.git",
    "directory": "packages/core"
  },
  "keywords": [
    "agno",
    "agent",
    "ai",
    "client",
    "streaming",
    "hitl",
    "tool-execution"
  ],
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --format cjs --packages external --sourcemap=external && bun build src/index.ts --outdir dist --format esm --packages external --entry-naming '[name].mjs' --sourcemap=external && tsc -p tsconfig.build.json",
    "dev": "bun build src/index.ts --outdir dist --format esm --packages external --entry-naming '[name].mjs' --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rodrigocoliveira/agno-types": "workspace:*",
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
